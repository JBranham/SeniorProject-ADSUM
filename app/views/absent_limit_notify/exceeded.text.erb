Dear <% @user.name %>,

We have recently detected that student: <% student.name %>
has been absent over <%= student.attendances.count(:conditions => { :absent => true}) %>
times this semester.

This student exceeds the number of allowed absences

Thank You,
ADSUM
