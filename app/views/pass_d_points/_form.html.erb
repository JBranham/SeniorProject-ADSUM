<%= form_for @pass_d_point, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :course_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :course_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :student_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :student_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :descr, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :descr, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :designated_point_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :designated_point_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pass_d_points_path, :class => 'btn' %>
  </div>
<% end %>
